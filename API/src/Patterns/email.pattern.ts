

/**
  Patrón RegExp que garantiza que la cadena sea un email valido
**/
export const EMAIL_PATTERN: RegExp = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const EMAIL_PATTERN_STR:string = "^[^\s@]+@[^\s@]+\.[^\s@]+$";